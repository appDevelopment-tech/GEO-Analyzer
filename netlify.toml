[build]
  command = "npm run build"
  publish = ".next"

[functions]
  # Increase Node.js memory for chromium binary
  node_bundler = "esbuild"

  # Include @sparticuz/chromium native files
  included_files = ["node_modules/@sparticuz/chromium/bin/**"]
